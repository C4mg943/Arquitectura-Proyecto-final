# URL base del API Gateway (nginx) que orquesta los microservicios.
# Para desarrollo local con Docker Compose:
VITE_API_BASE_URL=http://localhost:8000/api

# Tiempo máximo (ms) por petición HTTP del cliente.
VITE_API_TIMEOUT=15000
